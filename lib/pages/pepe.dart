import 'package:flutter/material.dart';

class PepePage extends StatelessWidget {
  const PepePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: ListView(children: [Text("sads")]),
    );
  }
}
